apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: open-data-ai
  labels:
    app: web
spec:
  type: NodePort
  selector:
    app: web
  ports:
    - port: 8501
      targetPort: 8501
      nodePort: 30501
